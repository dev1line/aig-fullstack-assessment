/** Default number of reviews per page (cursor-based pagination). */
export const DEFAULT_PAGE_SIZE = 10;

/** Fallback API base URL when NEXT_PUBLIC_API_URL is not set (e.g. local dev). */
export const API_DEFAULT_BASE_URL = 'http://localhost:3001';
